# Build outputs (regenerated by build-image-runner.sh / cmake).
# The vendored 3rdparty/ libraries and model/ weights ARE tracked on purpose
# so the app builds and runs without fetching external blobs.
tennis-app/install/
tennis-app/build-rk3588-aarch64/
tennis-app/build/
tennis-app/build-*/
